services:
  - type: web
    name: discord-member-monitor
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python discord_member_monitor.py
    envVars:
      - key: DISCORD_TOKEN
        sync: false
      - key: WEBHOOK_URL
        sync: false
        
